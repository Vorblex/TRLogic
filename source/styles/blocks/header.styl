.header
    position fixed
    z-index 10
    min-width $containerMinWidth
    max-width $containerMaxWidth
    width 100%
    display flex
    height $headerHeight
    background $black
    centered(x)


.header__container
    display flex
    justify-content center
    align-items center
    width 100%